import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Heat Pumps Now - Sales Portal',
  description: 'Professional sales portal for Heat Pumps Now - transparent pricing, education-based selling, and seamless operations handoff.',
  keywords: 'heat pumps, HVAC, sales, energy efficiency, home comfort',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  );
}